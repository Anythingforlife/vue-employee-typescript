export const PATTERNS = {
    email: '^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$',
    firstName: '[a-zA-Z]',
    lastName: '[a-zA-Z ]+',
    postalCode: '[a-zA-Z0-9 -]+',
    phoneNumber: '[0-9]+',
    mobileNumber: '^[0-9]*$',
    city: '[a-zA-Z ]+',
    country: '[a-zA-Z ,-_()]+',
};
